const fs = require('fs');
const path = '/Users/woorim/.gemini/antigravity-ide/brain/2e1381e5-0ff2-4fc0-bc37-da6f40a5d77a/implementation_plan.md';

const content = `# 에디터 - 미리보기 정밀 스크롤 동기화 적용 계획

제안해드린 아이디어 중 가장 부작용이 없고 강력한 **구역별 비율 동기화 (Sectional Percentage Sync)** 방식을 적용합니다.

## 구현 원리
문서 전체의 스크롤 비율을 계산하는 대신, 문서 내에 존재하는 **제목(Heading) 태그들을 기준점(Anchor)으로 삼아 각 구역별로 독립적인 스크롤 비율을 계산**합니다.
1. 왼쪽 에디터(TipTap)와 오른쪽 미리보기(Markdown)에서 렌더링된 모든 제목 태그(\`<h1>\` ~ \`<h6>\`)의 위치를 추출합니다.
2. 만약 양쪽의 제목 개수가 정확히 일치한다면, 현재 스크롤이 어느 제목과 제목 사이에 있는지 계산합니다.
3. 해당 구역 내에서의 퍼센티지(%)를 구하여 반대쪽 패널의 동일한 구역 내 스크롤 위치에 적용합니다.
4. 만약 제목이 하나도 없거나(짧은 글), 사용자가 타이핑 중이라 일시적으로 개수가 다르다면 기존의 '전체 비율 동기화'로 자연스럽게 폴백(Fallback)합니다.

## 변경 대상 파일
### \`app/admin/AdminStudio.tsx\`
- **[MODIFY]** \`handleEditorScroll\` 및 \`handlePreviewScroll\` 함수
  - \`syncScroll\` 이라는 재사용 가능한 정밀 동기화 유틸리티 함수를 내부적으로 추가합니다.
  - 양쪽 DOM에서 \`h1 ~ h6\` 요소를 찾아 1:1 위치 매핑을 수행하는 로직을 적용합니다.

## 기대 효과
- 아무리 큰 이미지나 임베드 요소가 삽입되어 픽셀 높이가 크게 달라지더라도, 다음번 제목이 나오는 순간 스크롤 오차가 \`0\`으로 초기화됩니다.
- 내용이 매우 길어져도 제목 단위로 동기화가 끊어지므로 사용자는 거의 완벽하게 일치하는 스크롤 경험을 얻게 됩니다.
- 백엔드 DB 스키마나 마크다운 파서를 수정할 필요 없이 프론트엔드 단에서 즉각 적용할 수 있습니다.

> [!NOTE]
> 해당 방식을 적용하기 위해서는 진행을 승인해 주시면 됩니다.
`;

fs.writeFileSync(path, content, 'utf8');
console.log("Done");
